Next.js frontend for **AI Copilot** (the capstone). See the [project README](../README.md) for what this teaches and full setup instructions.

Quick start (backend must be running via `docker compose up` first — see the project README):

```bash
npm install
npm run dev
```
