Next.js frontend for **Semantic Search & Recommender**. See the [project README](../README.md) for what this teaches and full setup instructions.

Quick start (backend must be running on port 8000, and `python ingest.py` must have been run at least once — see the project README):

```bash
npm install
npm run dev
```
