# All optional. Missing a key just disables that provider's panel in the UI.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Only needed if Ollama isn't on the default local port.
OLLAMA_HOST=http://localhost:11434
