# Get a free token at https://huggingface.co/settings/tokens (read access is enough).
# Needed for the Hugging Face Inference SDK calls (classification, summarization).
HF_TOKEN=

# Only needed if Ollama isn't on the default local port.
OLLAMA_HOST=http://localhost:11434
